@model MyBookApp.Models.Loan

@{
    ViewData["Title"] = "Låna en bok";
}

<h1 class="text-center mb-5">Låna en bok</h1>

<div class="container mx-auto mw-70ch">
    <form asp-action="Create">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="form-group mb-2">
            <label asp-for="BookId" class="control-label">Bok</label>
            <select asp-for="BookId" class ="form-control" asp-items="ViewBag.BookId"></select>
        </div>
        <div class="form-group mb-2">
            <label asp-for="BorrowerName" class="control-label"></label>
            <input asp-for="BorrowerName" class="form-control" />
            <span asp-validation-for="BorrowerName" class="text-danger"></span>
        </div>
        <div class="form-group my-4 mx-auto w-fc">
            <input type="submit" value="Låna" class="btn btn-success" />
        </div>
    </form>
    <div>
    <a class="link-dark link-offset-2 link-underline-opacity-25 link-underline-opacity-100-hover" asp-action="Index">Tillbaka till listan</a>
</div>
</div>



@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
